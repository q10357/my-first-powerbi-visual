import * as React from "react";

class ReactCircleCard extends React.Component<{}> {
    render() {
        return (
            <div className="circleCard">
                Hello, React!
            </div>
        );
    }
}

export default ReactCircleCard;
